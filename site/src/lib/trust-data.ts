/**
 * Landing-rework PRD §4.4 truthfulness gate: real numbers only, or clearly marked illustrative —
 * no fabricated institutional endorsement ships un-flagged. openmaths is a small, independently
 * built project (see /about) with no institutional partnerships or audited usage metrics yet, so
 * every figure and every "subject" entry below is explicitly illustrative rather than a faked
 * specific claim (a made-up "500,000+ equations explained" would be a real, un-flagged lie; "every
 * major topic, covered" describes the product's actual scope and is true today).
 *
 * TODO(razin): once real usage data or any institutional/press relationship exists, replace the
 * `subjects` list with real logos and consider re-adding numeric stats — this file is the single
 * place to do that swap.
 */
export const TRUST_SUBJECTS = ["Algebra", "Geometry", "Calculus", "Statistics", "Trigonometry", "Graphing"] as const;
