// Relocated to packages/components (PRD "Split into app + server" P0) — thin re-export so every
// existing @/lib/utils import site in this app needs no changes.
export * from "@openmaths/components/lib/utils";
