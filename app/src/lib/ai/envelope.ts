// Relocated to packages/shared (PRD "Split into app + server" P0) — thin re-export so every
// existing `@/lib/ai/envelope` import site in this app needs no changes.
export * from "@openmaths/shared/ai/envelope";
