// Relocated to packages/shared (PRD "Split into app + server" P0) — thin re-export so every
// existing `@/lib/dsl/schema` import site in this app needs no changes.
export * from "@openmaths/shared/dsl/schema";
