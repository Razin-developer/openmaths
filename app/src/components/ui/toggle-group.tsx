// Relocated to packages/components (PRD "Split into app + server" P0) — thin re-export so every
// existing @/components/ui/toggle-group import site in this app needs no changes.
export * from "@openmaths/components/ui/toggle-group";
