// Relocated to packages/components (PRD "Split into app + server" P0) — thin re-export so every
// existing @/components/ui/tooltip import site in this app needs no changes.
export * from "@openmaths/components/ui/tooltip";
