// Relocated to packages/components (PRD "Split into app + server" P0) — thin re-export so every
// existing @/components/ui/badge import site in this app needs no changes.
export * from "@openmaths/components/ui/badge";
