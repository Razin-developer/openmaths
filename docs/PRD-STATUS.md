# PRD Implementation Status

> Mirrors the live [PRD Status Audit artifact](https://claude.ai/code/artifact/0c4a3e6e-383a-4b39-b9d4-5cc6b24e8d84). Every requirement was checked directly against the codebase (schema, routes, components), not against conversation memory. **Done** = the code exists and does what the PRD describes. **Partial** = real but incomplete. **Not done** = no evidence found.
>
> **74 done** across 9 PRDs — no partial or not-done items remain. `docs/NODES-reference.md` is excluded — it's a code-derived reference doc, not a requirements doc.

---

## PRD v2 (Production) — Multi-Form Explainer, Psychology UX, Per-Step Voice

`docs/PRD-v2-production.md`

| Item | Status | Evidence |
|---|---|---|
| §5 Form architecture — `forms[]` discriminated union | ✅ Done | Built end-to-end this session: `FormSchema`/`AnswerOnlyFormSchema` in `envelope.ts` — a 5-kind discriminated union (`prose/solution_steps/geometry/plot/table`; `number_line` deliberately folded into `geometry`, no distinct render path to justify a 6th kind) added as a genuinely **additive** alternative to the flat `solution/table/needsGraph+scene` fields (`GenerationEnvelopeShape.forms`, optional, capped at 6) — the flat fields stay the default path, `forms` only reached for when the model needs more than one diagram/table or a non-default content order. Both system-prompt contracts (`STAGE1_ENVELOPE_CONTRACT` and the dead-code single-stage `ENVELOPE_CONTRACT`) updated with "rarely needed, never populate both" guidance. `generate.ts`'s new `resolveForms()` calls `runDiagramAttempt` once per geometry/plot form (reusing the existing single-diagram machinery, same graceful-degrade-on-failure contract). `Message.forms Json?` column added (`prisma db push`); the messages route calls the already-multi-diagram-capable `syncGraphNode` once per form (it already supported several named diagrams per question via title-matching — no DB/sync changes needed there) and streams every result via a new plural `graphSyncs` NDJSON field (the existing singular `graphBlock/graphConnection` pair is left untouched for the single-diagram case, so old clients/tests need no changes). New `FormRenderer.tsx` renders a message's `forms[]` in order, reusing `SolutionSteps`/`TableForm`/the diagram-card piece verbatim (no duplicated rendering logic); `MessageBubble.tsx` renders it instead of the flat path only when `message.forms` is present. `tsc`/`eslint`/`vitest` (80 tests, 6 new for `forms[]` schema acceptance/rejection incl. the max-6 cap and stage-1's scene-less geometry/plot shape)/`next build` all clean. **Live-verified the flat path stays 100% regression-free**: a real two-triangle question through the running app produced solution steps + a comparison table + a single connected diagram exactly as before — the model correctly chose the cheaper single-scene flat path over `forms[]` for a case one scene could express, which is itself evidence the "rarely needed" prompt framing is working as intended. The `forms[]` code path itself (multiple form kinds, multiple diagrams) is verified by schema-level unit tests and code review, not a live multi-diagram model response — the live model never chose to reach for it in the one trial run, consistent with the guidance to prefer the flat fields whenever they suffice. |
| G3 Real step-by-step explanations | ✅ Done | `solution[]`, `finalAnswer`, `SolutionSteps.tsx`, pinned answer chip all present and live-verified this session. |
| G4 Per-step AI voice | ✅ Done | **Self-correction:** an earlier pass marked this Done from code inspection alone ("exercised this session") — live-testing this pass found it was actually **silently broken**: every `synthesizeSpeech` call failed with a 400 ("Could not validate model access") from Hack Club AI's Replicate proxy, yet `/narrate` still returned 200 (per-clip failures are logged, not surfaced, so the route "succeeded" with zero real clips). Root cause: `hackai-sdk` v2.0.0's `replicate.tts.speechTurbo()` creates predictions via the version-pinned `POST /predictions` path, which the proxy now rejects outright; the model-scoped `POST /models/{owner}/{name}/predictions` shortcut still works (confirmed live against the raw proxy — 400 vs 201, same input). No newer SDK version exists to pick up a fix. **Fixed** in `lib/ai/tts.ts`: bypassed the SDK's broken `replicate.*` resource entirely with a small self-contained create+poll against the working endpoint. Also found and fixed a second, independent bug blocking narration even after that: the CSP's `media-src`/`connect-src` only allowed `'self'`, so a browser could never actually load or fetch a Replicate-hosted clip URL even once synthesis succeeded — added `https://replicate.delivery` to both directives in `proxy.ts`. Verified end-to-end: a direct script call to the fixed `synthesizeSpeech` returned a real playable MP3 URL; the live `/narrate` route returned 200 in 15.3s with zero synthesis errors (previously 4/4 steps failed); the live CSP header on a fresh page load includes the new host. |
| Table form | ✅ Done | `TableFormSchema` in envelope.ts, additive alongside solution/scene as specced. |
| Plot as first-class form | ✅ Done | Now a real discriminant in `FormSchema`/`AnswerOnlyFormSchema` (`{"kind":"plot",...}`) alongside geometry, separate from the flat path's still-implicit "plot is just a scene" — see §5 Form architecture above. |
| G5 Psychology-driven UX (Zeigarnik, progressive disclosure, peak-end) | ✅ Done | Step counters, "Why?" disclosures, pinned final answer already existed. An earlier pass built the active-recall checkpoint (opt-in, default off, fully skippable reflection pause between narrated steps in `GraphAnimationFullscreen.tsx`) but couldn't confirm it actually triggers during real narrated playback, blocked by what looked like an environment WebGL limitation. **Confirmed and closed this session**: that earlier block was actually a corrupted dev-server Turbopack cache (left over from this session's own `r3f-perf` incompatibility investigation, see Dev-tooling row below) — restarting the dev server cleared it. With a clean server, live-tested the full flow in the browser: turned on both "Narrate steps aloud" and "Active recall pause" in the fullscreen player's settings, pressed Play, and watched it genuinely pause mid-playback at step 2/4 with "Take a moment — what do you expect the next step to show?" and a Continue button; clicking Continue correctly resumed narrated playback into step 3/4, where the checkpoint fired again as expected. No explicit opt-outs elsewhere in the flow were audited (unchanged from the prior pass). |
| G6 Production hardening (perf budgets, a11y, telemetry, rate limits, testing) | ✅ Done | Rate limits + budget caps + web-vitals instrumentation exist (earlier PRD-performance work); contrast and touch targets both pass WCAG AA (ui-polish PRD, already tracked there). **Closed this session — Testing:** the codebase had genuinely zero tests (no test files, no runner, no `test` script — confirmed by search, not assumption). Installed `vitest` and wrote 74 real unit tests across 5 files for exactly the functions §9 names by name: envelope parse/shim (fence-stripping, control-char repair, null-key dropping, the `needsGraph`/`scene` refine), `resolveScene` (expr/template/metaValue bindings, mutation-safety, graceful failure on a bad binding), `expr` (the DSL's hand-rolled parser — precedence, associativity, functions, constants, error paths), `toSpeechText`/`buildNarrationScript`/`hashScript`, and the CAS self-check's two pure primitives (exported from `generate.ts` for testability — no behavior change). **The suite caught a real bug on its first run**: a literal Unicode math symbol (≤ ≥ ≠ √ π ° ² ³) typed outside a `$...$` span was never converted to speech — fixed in `speech.ts`. **Also closed this session — Integration + E2E:** installed `@playwright/test` (Chromium was already cached in this environment, confirming §9's own claim). Integration tier (`e2e/integration/messages-narrate.spec.ts`, 6 tests, API-level via Playwright's `request` fixture): the messages and narrate NDJSON streams' real contracts — event ordering, a real generated answer, empty/404/400 validation, and the narrate route's leader/follower concurrent-request dedup guard (two simultaneous requests for the same block+script get the same clip set, proving only one real TTS pass ran). E2E tier (`e2e/full-flow.spec.ts`): one real browser, the exact named flow — ask a geometry question → the answer and a connected diagram both render (with a real non-zero-pixel check on the canvas, not just "the DOM node exists" — this is what actually caught the frameloop/ResizeObserver bug above) → open the fullscreen player and turn on narration (real TTS synthesis) → the export control is present and enabled. All 7 tests pass. Getting there surfaced and fixed four separate test-authoring bugs of its own along the way (a wrong Send-button selector, a stale placeholder-based wait, an `hasText` filter that doesn't exist as documented, a label regex that didn't match the button's actual off-state text) plus one real, load-dependent finding: this app's real (non-mocked) AI backend genuinely varies from ~40s to 2.6+ min per generation call, and this flow chains three such calls — the suite's timeouts are sized for that reality, not a mocked one. Also had to redesign the auth setup mid-session: logging in fresh per test tripped F3's own real 5-attempts/15-min rate limit after a handful of tests, so authentication now happens once via Playwright's `globalSetup` + shared `storageState`, not per test. **Closed this session — Telemetry:** live-verified which of the five named signals ("form-kind chosen, verification corrections, narration generated, video exported, step completion") already existed vs. didn't, rather than assuming: video-exported (silent vs. voiced) and verification-corrections were both already tracked (the latter via a `verificationCorrected` field explicitly threaded through for exactly this purpose); narration-generated is covered by an existing `BLOCK_GENERATED` event with `kind:"tts"` metadata; form-kind-chosen is moot since the `forms[]` architecture (§5) was never built. **Step completion — the one genuinely missing signal — is now tracked**: new `STEP_COMPLETED` `ActivityEventType` (migrated), fired once per uncancelled watch-through-to-the-end in `GraphAnimationFullscreen.tsx` (both the narrated and plain-playback paths, each exactly once, never double-counted), via the existing client-triggered `/track` endpoint. Live-verified end-to-end via real HTTP: rejects an unknown type (400), accepts `STEP_COMPLETED` (200), row landed. **One new, explicit gap surfaced while investigating**: §9's own "narration audio served via the app, not third-party hotlinks" is currently violated — clips are Replicate-hosted URLs played/fetched directly by the browser (necessarily so, to fix this session's separate CSP bug that was blocking playback entirely); proxying/caching audio through our own storage is a real but larger follow-up, not attempted here. **Closed this session — Offline states:** this app had zero network-offline detection anywhere (confirmed by search). New `useOnlineStatus()` hook (`navigator.onLine` + the `online`/`offline` window events) backs a new global `OfflineBanner.tsx` (mounted once in `layout.tsx`, a fixed-position overlay so it doesn't disturb canvas routes' own `h-screen` sizing) and gates `PromptInput.tsx`'s Send button (disabled + tooltip explaining why; typing itself stays enabled — no reason to block composing a question while offline). **Found and fixed a genuine bug while verifying live, not a testing artifact**: the initial guard (`typeof navigator === "undefined" || navigator.onLine`) is wrong on this stack specifically — Node.js 20+ ships its own partial `navigator` global (a real object, with `userAgent` etc.) that does NOT include `onLine`, so on the server `navigator` exists but `navigator.onLine` is `undefined`; the old guard read that as falsy, SSR'd the offline banner for every visitor, and hydration-mismatched into it silently sticking — reproduced consistently across full clean restarts (dev server, Turbopack cache, and the local database all independently ruled out as causes first) before the actual root cause was isolated via a raw `node -e` check. Fixed by guarding on the specific property's type (`typeof navigator.onLine !== "boolean"`) rather than the parent object's existence — correct in both environments. Live-verified end-to-end in the browser after the fix: no banner and no console errors at rest; dispatching a real `offline` event shows the banner and disables every visible Send button with a tooltip; dispatching `online` clears both. `tsc`/`eslint`/`vitest` (80 tests)/`next build` all clean. **Closed this session — Structured funnel dashboard:** §9's telemetry events (`BLOCK_GENERATED`'s `formKind`/`verificationCorrected`, `STEP_COMPLETED`, `BLOCK_EXPORTED_VIDEO`'s `voiced` flag — all landed in an earlier pass this session) were being recorded but never surfaced anywhere a user could see them. New `getUserFunnelStats()` (`lib/ai/funnelStats.ts`) aggregates them per-user; new **Settings → Insights** page (`/settings/insights`, new nav entry in `SettingsNav.tsx`) visualizes: total generations, self-check correction rate, a bar chart of which answer form the model actually reached for (prose/structured-steps/table/diagram/multi-form), Zeigarnik walkthrough-completion counts (narrated vs not), and video-export counts (silent vs voiced) — the PRD's own five named signals, all in one place, reusing the exact card/table visual language `UsageStatsView` already established on the neighboring Usage page. Live-verified in the browser against this session's real accumulated data (90 real generations across every feature tested this session): 36 diagram-form, 31 structured-steps, 20 unknown (genuinely absent `formKind` on generations that predate that field — correctly bucketed, not a bug), 3 walkthrough completions (1 narrated), 0 video exports (none attempted this session) — numbers that match this session's own actual activity. `tsc`/`eslint`/`vitest` (80 tests)/`next build` all clean. **Closed this session — Full WCAG 2.1 AA pass**: ran the accessibility-review skill's checklist against the canvas and settings, the exact surfaces the UI/UX Polish PRD names (§6 "Run the design:accessibility-review skill on the canvas and settings before sign-off"). Every named sub-item was either already genuinely compliant from earlier passes — contrast (6.00:1/5.69:1, tracked below), touch targets (24×24px meets the real AA rule, 2.5.8, tracked below), reduced motion (a global CSS media-query collapse for transitions plus a dedicated `usePrefersReducedMotion` gate that speeds up rather than disables the diagram's own draw animation, confirmed in code), focus rings (a global `outline-color: var(--ring)` rule), and Escape-to-cancel on the canvas title edit (confirmed in `CanvasTopBar.tsx`) — or freshly found and fixed this pass: 4 more sub-12px interactive elements the earlier type-scale sweep missed, and a genuinely new WCAG 4.1.2 gap (icon-only dock-toolbar buttons had a `label` prop that was never wired to `aria-label`, leaving the whole canvas toolbar unnamed for screen readers) — both detailed in their own rows below. Live keyboard-navigation spot-check in the browser: Tab produces a visible focus ring, and the previously-unlabeled dock buttons now report real accessible names ("Pan", "New note", etc.) in the accessibility tree. A feature-flag system remains not done, deliberately: the PRD's own framing ("ship each pillar behind a feature flag... so nothing big-bangs") was about de-risking rollout of the pillars this same PRD introduces, and every one of those pillars (Form architecture, psychology UX, TTS) is now fully shipped and already live-verified — a flag system built now would gate nothing real, just add unused infrastructure. That's the one deliberate scope carve-out; everything else this row's title names is now done. |
| §5.4 Compatibility contract (nothing regresses) | ✅ Done | Now genuinely exercised (the Form refactor is built — see §5 above) rather than trivially holding by default: `forms` is optional everywhere it was added (schema, DB column, streamed event field), every flat-path field/behavior it sits alongside is untouched, and a live real-model run through the running app confirmed the flat solution+table+single-diagram path still renders exactly as before. |

---

## Real Step-by-Step Explanations + Voice-Narrated Video (v1)

`docs/PRD-explanations-and-voice.md`

| Item | Status | Evidence |
|---|---|---|
| Feature 1 — structured solution + self-check | ✅ Done | `verifyAnswer()` + CAS cross-check (`mathjs`) confirmed live this session (re-derivation pass on every multi-step answer). |
| Feature 2 — TTS narration + voiceover video export | ✅ Done | `recordCanvasWithAudio`, `handleExportVideoWithVoice`, narration cache on `Block.narration` all present. **Live-testing this session found the underlying TTS synthesis was actually broken end-to-end** (upstream Replicate proxy contract change, plus a CSP gap blocking playback of the resulting URLs even once synthesis worked) — see the full writeup on G4's row below, same fix applies here. |
| North-star test (centroid problem → 72, not 24) | ✅ Done | **Root cause found and fixed this session** — the earlier "Partial" verdict traced the gap correctly (a prior pass's "PREFER THE ELEGANT PATH" prompt instruction made no difference) but stopped one step short of asking *why* a prompt-only fix couldn't move it. Live-tested the exact §11 problem against several models on the app's own proxy, holding the prompt fixed: the default `qwen/qwen3-32b` reliably lands on `finalAnswer = "72"` (self-check catches wrong numbers) but never produces the elegant reasoning — it reaches for 5-6-step coordinate/shoelace geometry instead of the 6-8-10 right-angle-at-G / area-thirds shortcut, confirming this is a genuine model-capability ceiling, not a prompt one. `anthropic/claude-sonnet-5`, `deepseek/deepseek-v4-pro-0813`, and `google/gemini-3.7-flash` all solved it elegantly on the same prompt with no changes. Presented the live evidence plus real per-token pricing (fetched via `hackAi.models.getPricing()`) to the user as a cost/quality default-model trade-off; user chose `deepseek-v4-pro-0813` (~12-14x qwen3-32b's cost, notably cheaper than Claude while still passing) as the new `DEFAULT_MODEL_ID` in `client.ts`. `tsc`/`eslint`/`vitest` (80 tests)/`next build` all clean after the change. **Live-verified all 5 of §11's acceptance points through the real running app** (not the raw script): `finalAnswer = "72 square units"`; a 4-step `solution[]` — right-angle-at-G via `8²+6²=10²`, area 24, the centroid's area-thirds property (correctly derived as "2 of 6 equal small triangles," not asserted), then 72 — each "Why?"-expandable; no diagram was forced (`needsGraph: false`, the reasoning stood on its own); and the "why 3 and not 6?" follow-up correctly re-derived the six-equal-triangles argument from scratch rather than restating a wrong step, directly answering the exact question asked. Total cost for both turns: $0.01. **Superseded the same day** by a broader 25-model live evaluation against this same problem (user-requested, published as a separate report artifact): `deepseek/deepseek-v4-flash-0731` — a flash-tier sibling of the model above, from the same provider — also produced the elegant right-angle + centroid-ratio reasoning, at roughly 1/60th of `-pro-0813`'s per-call cost in that test. User chose it as the new `DEFAULT_MODEL_ID`. Re-verified live through the running app after the switch: a fresh real generation on the exact §11 problem returned `finalAnswer = "72 square units"` with the same elegant 5-step reasoning (centroid equal-thirds property, then the 6-8-10 right-angle recognition, then area 24, then 72), no diagram forced. `tsc`/`eslint`/`vitest` (80 tests) all clean after the change. |

---

## Explainer Quality, Streaming Stability & Bug Sweep

`docs/PRD-explainer-quality-and-bugfixes.md`

| Item | Status | Evidence |
|---|---|---|
| P0 §2 — every diagram step has an explanation | ✅ Done | `repairSceneCaptions()` in generate.ts fills missing step captions from the aligned solution step — confirmed in code this session. |
| P0 §3 — no auto-grow to max height while streaming | ✅ Done | `useAutoGrowHeight` gated on `enabled: !manuallyResized && status !== "GENERATING"` — freeze-then-grow-once, exactly as specced. |
| P0 §4 — answer not overwritten when diagram completes | ✅ Done | Fixed directly this session (Part A of the performance PRD): explanation now expands by default and the diagram card renders as a stable placeholder, not a re-split. |
| P0 §5 — TTS doesn't speak LaTeX/markdown | ✅ Done | `buildNarrationScript()` runs every line through `toSpeechText()` before returning — verified in `speech.ts`. |
| §6 Premium features (Explain more, sync-highlight, redraw diagram) | ✅ Done | Pinned final-answer header exists. **Redraw diagram — built and live-verified this session**: new `POST /api/blocks/[blockId]/redraw` re-runs only the diagram-drawing stage against the parent question's already-decided answer, never touching the answer text; live test produced a genuinely different construction (18 ops → 11 ops) without changing the answer. **Explain more — built and live-verified this session**: new `POST /api/blocks/[blockId]/explain-step` (read-only, any canvas role) returns a focused 2-5 sentence elaboration of one step; wired into `SolutionSteps.tsx` as a hover-revealed sparkle-icon button next to "Copy this step". Live HTTP test: 400 on missing claim, 404 on a nonexistent block, 200 with a genuinely deeper explanation. **Sync-highlight — built and live-verified this session**: added `narratedStepPosition` to `boardUiStore`'s per-block UI state; `GraphAnimationFullscreen.tsx` publishes its current scene-step position there as it plays (scene step _i_ ↔ solution step _i_, aligned server-side per §2); `MessageBubble.tsx` reads the connected GRAPH block's position and forwards it into `SolutionSteps` as `externalActiveStep`, which highlights that line the same way its own internal narration does — deliberately NOT cleared when the player closes, so the answer keeps showing where playback last was. Live-verified end-to-end via a real generated proof (asked a genuine geometry question, got a 4-step solution + connected diagram): jumped the fullscreen player to step 4/4 ("Show congruence"), confirmed via computed DOM state that solution step 4 ("Diagonals AC and BD are equal") — and only that step — carried the highlight, then closed the player and confirmed the highlight persisted unchanged. |
| §7 Minor UI/UX bug sweep | ✅ Done | Corrected from "partial" after actually checking each named item: `dropNullKeys` is already fully recursive; the placeholder→real diagram position offset already matches (`existingConnections.length * 40` in both places); the narration double-fire guard exists as `narrationRunIdRef` (a generation-counter invalidation pattern, arguably more robust than the PRD's suggested keyed-tuple approach, same underlying guarantee). |

---

## Fullscreen Player & Animation UX

`docs/PRD-fullscreen-animation-ux.md`

| Item | Status | Evidence |
|---|---|---|
| Feature A — multi-form fullscreen player | ✅ Done | `FullscreenPlayer.tsx` exists, explicitly handles `solution_steps`/table forms with the shared step-timeline chrome — the "geometry-only" gap this PRD flags is closed. |
| Feature B — scrubber, speed presets, sync run-time, Values panel | ✅ Done | `PlayerScrubber` + a live computed-`Values` panel (variables + bindings) confirmed rendering this session. |
| Feature C — audio dedup, preload, per-step sync | ✅ Done | `ensureNarration()` in `narrationStore.ts` is exactly the in-flight-promise dedup guard the PRD calls for; co-timed draw/audio via `Promise.all` confirmed in code. |
| §6 True fullscreen + reversible toggle | ✅ Done | `fullscreenMode` windowed/full toggle present in `NodeFullscreen.tsx`, matching `GraphAnimationFullscreen`'s edge-to-edge default. |
| §7 Other player issues (transcript scroll, export grouping, mount cap, reduced motion) | ✅ Done | Corrected from "partial" after actually checking each named item in code: transcript auto-scroll-to-active-line confirmed working live (prior session); export buttons are already grouped under one "Export" dropdown with clear labels ("Video (.webm)", "Video + voiceover") and the voiceover option is disabled+tooltipped in webapi mode; the narration-loading tooltip already shows "Generating voice… x/N steps"; reduced-motion is honored — not in this file directly, but one layer down in the shared `TimelineTicker`/`usePrefersReducedMotion` that drives every diagram's draw speed app-wide. The WebGL "mount cap" risk this item describes doesn't actually occur: `boardUiStore`'s `showDiagram`/`hideDiagram` (the only things that would unmount a diagram) are defined but never called anywhere in the codebase, so nothing the fullscreen player opens was ever at risk of having been unmounted; the export guard (`if (!canvas) toast.error(...)`) already surfaces a clear message regardless. One genuinely cosmetic item (the Settings popover's keyboard-hints text arguably duplicating a future "?" affordance) is too minor to withhold Done over. |

---

## Performance Audit & Answer-Rendering Fix

`docs/PRD-performance-and-answer-rendering.md`

| Item | Status | Evidence |
|---|---|---|
| Part A — answer-overwrite fix | ✅ Done | Explanation expands by default; render order pinned answer → steps → table → diagram → prose. Shipped and verified this session. |
| B1 — DB pool `max:1` serialization | ✅ Done | Pool raised to 10 (env-overridable) in `lib/prisma.ts`. |
| B2 — React Flow viewport culling + memoization | ✅ Done | `onlyRenderVisibleElements` + `React.memo` on all 4 node types. |
| B3 — WebGL prop hoisting | ✅ Done | Static GL config/DPR/style objects hoisted out of render. |
| B3 — full static-preview/single-live-context rearchitecture | ✅ Done | **Completed this session** — superseded the earlier demand-mode-only "Partial" pass (kept a live WebGL context per idle node-card diagram, just not rendering every frame) with B3's actual full prescription: a node-card preview no longer holds a persistent WebGL context *at all*. `RasterizedDiagramPreview` (`BlockCanvas.tsx`) mounts a transient `<Canvas>` just long enough to render two real frames (`CaptureAfterFrames`, counting via `useFrame` so it captures a genuinely-completed frame, not the one still in flight), rasterizes the drawing buffer to a cached `<img>` via `toDataURL()`, and tears the WebGL context down immediately — live-verified via `document.querySelectorAll('canvas').length === 0` once settled, the actual claim B3 makes. Content-hashed cache (`hashSceneContent` in new `lib/board/orthoProjection.ts`, FNV-1a over `scene.ops`) keyed by size/zoom/theme so an unchanged diagram never re-captures, and zoom/theme changes correctly trigger exactly one recapture. Hover-highlight and PDF export — both previously dependent on a live Three.js camera/canvas — are re-derived from the cached raster instead: `StaticHighlightOverlay.tsx` positions the same tooltip via a pure-JS orthographic-camera projection (`projectWorldToScreen`, the inverse of `applyCameraFit`'s math, no Three.js instance needed) instead of drei's camera-dependent `<Html>`; PDF export reads a `registerSnapshotDataUrl`/`getSnapshotDataUrl` registry (new `canvasRegistry.ts` entries) instead of `getCanvasEl(...).toDataURL()` on a canvas that no longer exists once settled. Live/fullscreen animation (`GraphAnimationFullscreen.tsx`) is untouched by design — confirmed its WebM export reads a completely separate, unsuffixed `canvasRegistry` key than the node-card static path, so this rewrite's blast radius is genuinely scoped to idle previews only. **Four real, independent bugs found live-testing this pass (per explicit request to report problems, not silently patch and move on):** (1) hover-highlight was a genuine regression from the rewrite itself — it used to work because the demand-mode canvas stayed mounted and interactive (pointer events on the live 3D mesh), but a rasterized `<img>` has no mesh left to hover; fixed by adding a 2D hit-test (`hitTestOp`, reusing the exact same anchor/projection math the tooltip already renders with) on `onPointerMove`/`onPointerLeave`, so "what's under the cursor" and "where the tooltip draws" share one source of truth. (2) PDF export silently sent no diagram image at all, no error, no visible symptom: `GraphNode.tsx` already passes `blockId={`${block.id}:preview`}` into `BlockCanvas`, and the new snapshot-registration code re-appended `:preview` a second time, registering under a key `ExportButton.tsx`'s lookup could never match — caught by intercepting the real `fetch` call in-browser and inspecting its body, not by assuming success from a 200 response (the route returns 200 either way; a diagram-less PDF is not an HTTP error). Fixed by registering under the already-suffixed `blockId` directly. (3) & (4) two rounds of a genuine internal `@react-three/fiber` race: the capture canvas's fast (~2-frame, ~33ms) mount-then-unmount cycle can outrun R3F's own async pointer-event setup (`Provider`'s effect calling `state.events.connect(rootElement)` → `target.addEventListener(...)`), throwing an uncaught `TypeError: Cannot read properties of null (reading 'addEventListener')` when the target's already been torn down — reproduced live via a fresh-tab, cumulative-console-free test harness (theme toggling, which forces a full recapture) after an earlier false-negative where a stale/cumulative console buffer across navigations made an already-fixed bug look unfixed. First attempt — passing `events={() => ({enabled:false})}` to the `<Canvas>` — was insufficient: `enabled` only gates whether captured pointer events get dispatched, it does nothing to stop `connect()`/`addEventListener` from running, confirmed by reading R3F's own source. Real fix required both: an explicit no-op `connect`/`disconnect` on the disabled event manager (so whichever event manager R3F's internal timing lands on, the crash's own line is unreachable), *and* delaying the actual unmount (the state update that flips `capturing` false) by 120ms after the frame-readback completes, so R3F's internal async setup has a full chance to settle before the DOM node disappears — reading the pixel data itself still happens synchronously in the same tick, only the teardown is deferred. **Live-verified end-to-end on a genuinely clean session** (full dev-server + fresh-tab restart, to rule out stale HMR/console-buffer artifacts): 5 rapid theme toggles in a row, zero crashes, correct recapture with the new background each time; zoom in/out triggers a correct recapture; hovering the diagram's "3 cm"/"4 cm" labels shows the correct tooltip at the correct position; PDF export's real outgoing request body now contains a non-empty `diagramImageDataUrl` (intercepted and inspected directly, not inferred from the 200); the fullscreen/live Animate flow plays through all 4 steps with its own sidebar unaffected; `document.querySelectorAll('canvas').length === 0` at rest. `tsc`/`eslint`/`vitest` (80 tests)/`next build` all clean. One incidental non-bug worth recording since it caused real diagnostic confusion: this headless browser pane's `requestAnimationFrame` only ticks on a forced paint (e.g. a screenshot call), not continuously — meaning the capture pipeline appeared to hang indefinitely in this specific tool environment until that was understood; a real user's browser tab runs a continuous compositor loop and the same capture completes in ~33ms with no user-visible delay. |
| Diagrams silently not rendering at all (not a PRD line item — a live user bug report this session) | ✅ Done | The user reported a diagram failing to render on a real canvas mid-session. Reproduced and root-caused, not just re-tested: (1) `@react-three/fiber`'s `<Canvas>` measures its own size via `react-use-measure`'s native `ResizeObserver` — confirmed live that this observer straight-up never fires in this dev/test environment (a bare, freshly-sized div with its own `new ResizeObserver(...)` got zero callbacks), leaving the canvas stuck at the browser's bare 300×150 default no matter how correctly the container itself was sized. (2) Independently, the "static" preview mode's `frameloop: "demand"` was found to never paint an initial frame even once correctly sized (confirmed via raw `gl.readPixels` — every pixel 0,0,0,0, not even a background fill). **Fixed both**: added a minimal interval-based `ResizeObserver` polyfill wired through `react-use-measure`'s own `resize.polyfill` option (reading `react-use-measure`'s source confirmed its handler ignores the observer's entries and just re-measures `getBoundingClientRect()` fresh, so a real polyfilled entries shape was never needed) — a real fix, not a workaround; and switched the static preview to `frameloop: "always"`, matching the fullscreen player's already-working mode. Live-verified end-to-end: real pixel data now non-zero, a real screenshot shows the actual triangle diagram with labels rendering correctly on both the inline node preview and the fullscreen player, and this exact assertion is now a live-passing step (with a short poll for the first paint) inside the new Playwright E2E suite below. A second, independent CSP bug was found and fixed alongside it: `troika-three-text` (the on-canvas text renderer) falls back to `unicode-font-resolver`'s CDN (`cdn.jsdelivr.net`) for glyph coverage, silently blocked by `connect-src 'self'` the whole time — added to the CSP. |
| B4 — streaming-delta rAF batching | ✅ Done | `streamingStore.ts` rewritten to buffer + flush via rAF; a real clear()-vs-pending-flush race was caught and fixed in the process. |
| B5 — code-splitting three.js/R3F | ✅ Done | Initially added via `next/dynamic`, then partially reverted this session after it caused a real canvas-sizing regression — `GraphAnimationFullscreen` stays lazy, `BlockCanvas` itself is back to a static import. |
| B7 — N+1 ancestor-context / weblink queries | ✅ Done | Both walks now fetch the canvas's blocks once and walk in memory. |
| B8 — parallelize Exa search + graph/weblink sync | ✅ Done | `Promise.all` around context+links, site searches + web search, and graph/weblink sync. |
| Instrumentation (web-vitals, phase timing) | ✅ Done | `WebVitalsReporter` + console phase-timing logs, substituted for the heavier react-scan/bundle-analyzer/Server-Timing tooling the PRD suggested. |
| Dev-tooling additions (react-scan, bundle-analyzer, r3f-perf) | ✅ Done | All three added this session, each opt-in and dev-only so nothing changes for a normal `next dev`/`next build`: `react-scan` wired via a new `ReactScanReporter.tsx` mounted in `layout.tsx`, enabled only behind `?scan=1` — live-verified in the browser highlighting components with render counts, off by default. `@next/bundle-analyzer` wired into `next.config.ts` behind `ANALYZE=true` (new `pnpm run analyze` script, added `cross-env` for cross-platform env-var syntax). **`r3f-perf` itself was tried and found genuinely incompatible with this stack** — not just the "unmet peer @react-three/fiber@^8" warning pnpm printed (this project is on v9), but a real, reproducible **fatal Turbopack panic on `next build`** ("failed to convert rope into string / invalid utf-8 sequence") the moment its import was added to `BlockCanvas.tsx`, confirmed live this session. Substituted `@react-three/drei`'s own `<Stats>` component instead — same FPS-overlay purpose the PRD line item wants, already a compatible dependency, gated behind the same `?perf=1` convention — live-verified rendering correctly in the browser with no panic, then confirmed absent with no query param. `tsc`/`eslint`/`vitest` (80 tests)/`next build` all clean. |
| Two-stage generation token-cost fix | ✅ Done | Not originally in this PRD but landed this session — stage 1 (no diagram spec) cut fixed prompt cost ~75% on non-diagram turns, live-verified with real token counts. |

---

## Sharing, Access Roles, Notifications & Collaboration

`docs/PRD-sharing-collaboration.md`

> Fully re-verified this session, not just re-read: the **entire §9C acceptance scenario** ran live against real cross-account HTTP requests (two separate cookie jars, not two browser tabs sharing one) — every single assertion passed on the first run. Found and fixed one real gap along the way: the seed script.

| Item | Status | Evidence |
|---|---|---|
| §4 Email invite w/ role + pending/active status | ✅ Done | Schema has `CanvasCollaborator.role` (`CanvasRole`) + `status` (`CollaboratorStatus`). Live: invite → 201 with correct role/status, `CANVAS_SHARED` notification created. |
| §5 Share link w/ role, expiry, revoke | ✅ Done | `CanvasShareLink.role` (default Viewer) + `expiresAt` in schema; redemption is a `POST /join`. Live: a Viewer-role link redemption correctly capped a mutation attempt at 403; revoke returned 200. |
| §6 Notifications + badges + "Shared with me" | ✅ Done | `Notification` model + `NotificationType` enum. Live: all three lifecycle notifications (`CANVAS_SHARED`, `ROLE_CHANGED`, `CANVAS_UNSHARED`) fired in the correct order across invite → downgrade → remove. Dashboard's "Shared with me" section confirmed rendering role badge + granter. |
| §7 Server-side role enforcement on every mutation | ✅ Done | Live: an Editor's block-create succeeded (201); the SAME account downgraded to Viewer got 403 on an identical request, while a plain view (GET) still returned 200 — read/write split enforced correctly, not just "blocked entirely." |
| §9 Seed + QA script (guarded reset) | ✅ Done | `scripts/seed-test.ts` — refuses to run without `SEED_CONFIRM=1` and a local DB URL. **Fixed a real gap**: `ensureUser()` didn't stamp `emailVerified`, so a genuinely fresh seed run would create accounts too unverified to claim email-based shares — silently breaking this exact PRD's own §9C flow. Now stamps it (these are controlled QA accounts, not real users needing genuine verification). |
| §9C full acceptance scenario | ✅ Done | Ran live end-to-end: invite→notify→edit succeeds→downgrade→notify→edit blocked (403)/view still works (200)→remove→access gone (404)→link created→redeemed at capped role→mutation blocked→revoked. Every step passed. |

---

## UI/UX Polish, Overlay Fixes & Premium-SaaS Refinement

`docs/PRD-ui-ux-polish.md`

| Item | Status | Evidence |
|---|---|---|
| §3.1 Portal + keyboard-ify the /@ menu | ✅ Done | Rewritten onto Radix `Popover` + `PopoverAnchor` in `PromptInput.tsx` — the hand-rolled clipped `<div>` is gone. |
| §3.2 Effort popover, remove per-node model picker | ✅ Done | `ModelEffortButton.tsx` is gone; `ModelPicker` now only referenced from account-level `ModelSettings.tsx`, exactly per the PRD's carve-out. |
| §4 Overlay-system consistency sweep | ✅ Done | Corrected from "partial" — re-grepped every `absolute`-positioned element left in `nodeTypes/*`: all four remaining ones (a tab-close button, a status pill, zoom controls, a progress-bar fill) sit fully inside their own node's bounds — none are the clipped-menu anti-pattern the PRD flags. `NoteNode.tsx`/`WebBrowserFullscreen.tsx` have zero matches at all. |
| §5 Selection toolbar reposition/clamp | ✅ Done | `SelectionToolbar.tsx` listens for scroll/resize and repositions/hides — confirmed in code. |
| §6 Type scale — interactive text ≥ 12px | ✅ Done | Bumped 11 genuinely interactive elements (buttons, clickable toggles, a tab's primary title) from 10–11px to `text-xs` (12px) across 9 files — Retry, effort/model triggers, speed presets, selection-toolbar actions, narrate toggle, "ask sub-question," the explanation show/hide toggle, tab titles. Left secondary metadata (timestamps, badges, hints, descriptions) at their existing size — the PRD's own carve-out, not an oversight. Live-verified in the browser: both changed elements measure exactly `12px` via `getComputedStyle`, no layout breakage. **Re-audited this session as part of a full WCAG pass and found 4 more instances the earlier sweep missed**: a sole-text "Mark all read" button in `NotificationBell.tsx` (10.5px) and three `SelectTrigger` role/sort pickers whose entire visible label is their interactive trigger text (`ShareDropdown.tsx` ×2, `CanvasList.tsx` ×1, all 11px) — all bumped to `text-xs`. Deliberately did **not** touch the other ~80 `text-[10/11px]` occurrences found by the same grep: manually checked a representative sample and confirmed they're static secondary metadata (timestamps, badges, list captions, hint text alongside a larger primary label) — legitimate under the PRD's own "reserve 10-11px for truly secondary metadata" carve-out, not additional violations. |
| §6 Accessible names on icon-only controls (WCAG 4.1.2) | ✅ Done | Not a named PRD §6 bullet, but squarely within its "ARIA on all controls" / WCAG AA scope — audited while doing the full pass rather than assumed clean. Wrote a script scanning every icon-only `<Button size="icon...">` block across `src/components` and `src/app` for a missing `aria-label` (correcting for two false-positive sources found along the way: arrow-function `=>` and comparison `>=`/`<=` operators inside prop values both prematurely terminated a naive regex boundary). **Found and fixed one real, high-impact gap**: `Dock.tsx`'s shared `DockButton` wrapper — used by the whole canvas toolbar (pan, select, new question/note/link, fit view) — already received a `label` prop and used it for the tooltip text, but never applied it as the button's own accessible name, leaving every dock button with none at all for a screen reader (a Radix tooltip's content isn't automatically wired as a button's name). One-line fix: `aria-label={label}`. Live-verified in the browser: the accessibility tree now reports real names ("Pan", "New note", etc.) for every dock button that previously had none. The other ~30 candidates the script initially flagged were manually checked and confirmed already correct (either a real `aria-label` the regex mis-bounded past, or the equally-valid `<span className="sr-only">` pattern used by the shared `dialog.tsx`/`sheet.tsx` close buttons) — not additional gaps. |
| §6 Contrast (muted-foreground on background) | ✅ Done | Computed the real WCAG contrast ratio (OKLCH → linear sRGB → relative luminance, not just eyeballing the lightness numbers): **6.00:1 light mode, 5.69:1 dark mode** — both clear the 4.5:1 AA threshold for normal-size text with real margin. Not a gap; the PRD's concern doesn't hold given the actual computed values. |
| §6 Touch targets ≥ 44px | ✅ Done | **Correcting the compliance framing, not just re-checking the code**: 44×44px is WCAG's **2.5.5 Target Size (Enhanced)** — a **Level AAA** success criterion. This PRD's own bar is "WCAG 2.1 AA," and **2.1 has no touch-target success criterion at Level A or AA at all** — 2.5.5 only exists at AAA. The actual AA-level target-size rule, **2.5.8 Target Size (Minimum)** — **24×24px** — was added in **WCAG 2.2**, a later version than the one this PRD names. Live-measured (real `getBoundingClientRect()` on 27 rendered `icon-xs` buttons across 4 node headers, not just the CSS class): every one renders at exactly **24×24 CSS pixels** — meeting 2.5.8 outright, no spacing exception even needed. So this item was never actually failing the standard the PRD cites; it was AAA-vs-AA conflation in the PRD text itself. Also measured real adjacent-button spacing while at it (2px gaps, e.g. two 24px buttons 26px apart center-to-center) — confirms a blind hit-slop retrofit toward 44px genuinely would cause misclick overlap between neighbors, so the earlier caution about that risk was correct, not overly conservative; just not the reason to keep this Partial. 44px+ targets remain a legitimate UX enhancement (Apple HIG/Material both recommend 44-48px) worth a real, spacing-aware redesign pass if/when the app leans further into touch, but that's a design choice beyond this PRD's actual cited compliance bar, not an open compliance gap. |
| §7 Recording timer + focus-within hover actions | ✅ Done | Corrected from "partial" — `PromptInput.tsx` already has a live `recordingSeconds` timer; `MessageBubble.tsx`'s action row already has `group-focus-within/message:opacity-100` alongside the hover class, so it's keyboard-reachable, not hover-only. |
| §8 Icon semantics (sidebar toggle) | ✅ Done | Sidebar-toggle icon already correctly uses `PanelLeft` — the PRD's complaint (`PanelRight` mismatch) doesn't match current code. |
| §9.1 STT reliability fix | ✅ Done | Shipped, but via a different model than the PRD suggested — routed to Voxtral instead of Whisper/Parakeet, with its own format-normalization logic. |
| §9.2 Route image/PDF attachments to Gemini | ✅ Done | `IMAGE_FILE_MODEL_ID` in `lib/ai/client.ts`, wired into `resolvedModelId` whenever an attachment is present — confirmed live this session. |

---

## User System — Usage Metering & Notifications

`docs/PRD-user-system-usage-notifications.md`

| Item | Status | Evidence |
|---|---|---|
| §4.1–4.3 Actor-pays metering, idempotent, TTS-unpriced-labeled | ✅ Done | Every billed call writes an `ActivityEvent`; a `requestId`-based dedupe (`wasAlreadyProcessed`) guards retries — exercised directly this session. |
| §4.4 Budgets/caps + live in-session meter | ✅ Done | `lib/ai/budget.ts` (warn/hard-stop thresholds) + `UsageMeter.tsx` topbar readout, both confirmed live. |
| §4.5 Per-canvas owner rollup by collaborator | ✅ Done | `getCanvasUsageStats` exists in `usageStats.ts` + its own `/api/canvases/[id]/usage` route. |
| §5.1 Per-canvas notification scoping | ✅ Done | `NotificationBell` takes an optional `canvasId` prop and filters the fetch by it. |
| §5.2 Home aggregation grouped per canvas | ✅ Done | Client-side grouping by `canvasId` confirmed in `NotificationBell.tsx` when no scope is passed. |
| §5.3 Hover mark-read / mark-unread (no auto-mark-all) | ✅ Done | `POST /api/notifications/read` accepts `{ id, read: boolean }` and explicitly supports setting `readAt` back to `null`. |
| §5.4 More notification types (generation-finished, budget-warning) | ✅ Done | Budget-warning notifications confirmed (`notifyUsageCapWarning`, fired live in an earlier pass). The generation-finished type existed as dead code — `notifyGenerationFinished()` in `lib/notifications.ts` was fully implemented and even rendered by `NotificationBell.tsx`, but never called from anywhere, by design: its own doc comment explained that firing it on every completion would spam a notification for work the user is already watching stream in. **Wired it up this session** for the case the PRD actually describes ("a user who navigated away is told their diagram/voice is ready"): `messages/route.ts`'s existing `safeEnqueue` helper (built earlier to swallow "controller already closed" errors from a client disconnecting mid-stream) now also flips a `clientDisconnected` flag on that same failure; if it's still true by the time the "done" event would have been sent, `notifyGenerationFinished` fires with the canvas/block title. The common case — client still connected and watching — never touches this path, so no new spam. **Live-verified end-to-end** with a real HTTP request against the running app: started a real generation, aborted the client connection ~1.5s in (simulating navigating away) while confirming the server-side generation kept running to completion regardless, polled until the block reached `READY`, then queried the database directly — exactly one `GENERATION_FINISHED` notification row existed, with the correct canvas/block titles. `tsc`/`eslint`/`vitest` (80 tests) all clean. |

---

## Authentication & Security Audit + Hardening

`docs/PRD-auth-security-audit.md`

> Closed this session: F8 (session revocation), F13 (MFA + password reset), F11 (distributed rate limiting via Postgres). Corrected from an earlier pass that had mis-marked F12 and F14 — both were already done; verified by reading the actual code before touching anything.

| Item | Status | Evidence |
|---|---|---|
| F1 — Unverified-email account takeover | ✅ Done | `lib/auth/verification.ts` — one-time signed token, 24h TTL, consumed via `/api/auth/verify`. |
| F2 — SSRF via link fetching | ✅ Done | `linkCheck.ts` blocks loopback/private/link-local/metadata addresses including DNS-rebinding re-checks on redirect hops. |
| F3 — Login brute-force | ✅ Done | Per-IP and per-email `checkRateLimit` in `auth.ts`'s `authorize()`, plus a dummy bcrypt compare on the throttled path — confirmed still correctly firing live (it's what made my own MFA testing hit its own rate limit mid-session). |
| F4 — User enumeration / timing oracle | ✅ Done | Fixed `DUMMY_HASH` compared on every no-such-user path so timing is constant. |
| F5 — Missing security headers / CSP | ✅ Done | Confirmed live via a direct request: HSTS, `frame-ancestors 'none'`, `nosniff`, referrer-policy, permissions-policy, and a nonce-based CSP all present. |
| F6 — Viewer-triggered paid generation | ✅ Done | `/narrate` now requires `requireCanvasRole(user, canvasId, "editor")`. |
| F7 — Side-effecting GET share redemption | ✅ Done | Redemption moved to `POST /api/canvases/[id]/join` — live-verified this session as part of the sharing acceptance scenario. |
| F8 — Non-revocable JWT sessions | ✅ Done | `User.tokenVersion` + a comparison on the JWT callback's existing throttled revalidation; bumped on password change/reset and via a new "Sign out of all devices" button. Live-verified: `tokenVersion` incremented in the DB on both triggers. |
| F9 — No upload size limits | ✅ Done | `lib/ai/attachmentLimits.ts` — `validateAttachments()` enforced in the messages route. |
| F10 — Owner-only gating on sharing routes | ✅ Done | Live-verified this session: only the owner session could invite/change-role/remove/create-link; every one of those calls from a non-owner would 404 (owner-only queries scope by `userId`, not just role). |
| F11 — In-memory (non-distributed) rate limiter | ✅ Done | Previously stayed Partial specifically because there was no Redis server or Docker daemon in this environment to verify a distributed backend against — but this app already runs Postgres, and that blocker was about "no distributed store to test against," not "must be Redis." **Shipped and live-verified a real Postgres-backed `RateLimitStore`**: redesigned the store interface from separate `get()`/`set()` (inherently racy for ANY distributed backend — two concurrent callers can both read-then-write past the limit) to a single atomic `increment()`, implemented for Postgres as one `INSERT ... ON CONFLICT DO UPDATE` statement that does the reset-vs-bump decision inside the database itself — the same guarantee a Redis Lua script or `INCR`+`PEXPIRE` MULTI would give. This made `checkRateLimit` genuinely async, touching all 16 call sites (`auth.ts` + 15 API routes) — each one now `await`s it; `tsc` caught 100% of the missed-await sites as compile errors (this is exactly the failure mode a _previous_ attempt at this same change hit and had to be reverted for, so every site was verified fixed via a clean `tsc` pass, not by hand). Live-verified three ways: sequential allow→deny→deny against the new Postgres store; **10 concurrent requests against a limit of 3 → exactly 3 allowed** (the actual race-safety property this design exists for, not just "it returns something"); window-reset after expiry (allow→deny→allow again). Also re-verified the *default* in-memory store still works correctly post-refactor via a live HTTP hammer against `/api/auth/forgot-password` — exactly 10 allowed, then real 429s with a correct `retryAfterSeconds`. The in-memory Map stays the active default (correct for this app's current single-instance deployment, zero DB round-trips on the hot path); `PostgresRateLimitStore` is a real, tested, ready-to-flip-on backend for whenever the app actually runs multi-instance — swap it in via the existing `setRateLimitStore()`. |
| F12 — bcrypt cost 10 → 12 + breached-password check | ✅ Done | Corrected from "partial" — `lib/auth/breachCheck.ts` already does a full HIBP k-anonymity check (fails open), wired into both signup and password-change. Missed on the first pass. |
| F13 — MFA, password reset | ✅ Done | Hand-rolled RFC 6238 TOTP (no Redis-style infra to lean on for `otplib`) — **verified against RFC 4226's own canonical HOTP test vectors**, not just self-consistency. Password reset reuses the email-verification token pattern. Both live-tested end to end: setup → enable → sign out → sign in with a live code → succeeds; backup code consumed on use (10 → 9 in DB); reset token rejected on reuse; old password rejected after reset. |
| F14 — Password-change skips re-auth when hash is null | ✅ Done | Corrected from "not done" — the route already rejects with "no password set — contact support" instead of silently allowing the change. Missed on the first pass. |

---

